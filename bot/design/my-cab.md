# /my_cab — Design Document (Личный кабинет)

> Статус: ❌ Не реализовано
> Источник: маркетинговый экспорт 2026-02-27

---

## Назначение

Личный кабинет пользователя. Хранение истории отчётов, доступ к PDF-резюме, управление аккаунтом.

---

## Разделы (5 блоков)

### 1. 📊 Мои отчёты
- История аудитов (дата + тип: Job / Clients / Brand + балл)
- Просмотр предыдущих результатов
- Скачать отчёт (PDF или Telegram-сообщение)
- Регенерация (1 раз в сутки, если профиль не изменился)

### 2. 📄 Мои резюме
- Список созданных ATS-резюме
- Под какую вакансию создавалось
- Дата создания
- Скачать PDF повторно

### 3. 💳 Мои покупки
- История Stars-транзакций
- Активные продукты (Full Audit, ATS Resume, и др.)
- Срок действия (если применимо)

### 4. ⚙️ Настройки
- Язык бота (RU / EN)
- Цель профиля по умолчанию (job / freelance / brand)
- Уведомления (вкл/выкл retention-сообщений)

### 5. 🔗 Пригласить друга (Referral)
- Уникальная реферальная ссылка
- Счётчик приглашённых
- Бонусная механика (не определена — placeholder)

---

## Связь с backend

- Данные: `reports`, `requests`, `artifacts` в PostgreSQL
- Идентификатор: `platform_id` (Salebot ID пользователя)

```sql
-- История отчётов
SELECT r.id, r.created_at, r.analysis_type, rep.result_json
FROM requests r
LEFT JOIN reports rep ON rep.request_id = r.id
WHERE r.platform_id = $1
ORDER BY r.created_at DESC;
```

---

## Backend задачи (не спроектированы)

- API endpoint для получения истории: `GET /webhook/linkedin/cabinet`
- Хранение PDF-резюме: Telegram file_id → таблица `reports` (type = 'resume')
- Схема доступа к paid-функциям: флаг в `users` или отдельная `purchases` таблица
- Retention-нотификации: отдельный n8n scheduled workflow
- Реферальная логика: таблица `referrals` (referrer_id, invited_id, reward)
